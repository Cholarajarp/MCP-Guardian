[project]
name = "mcp-guardian-backend"
version = "0.1.0"
description = "MCP Guardian - static security scanner and Cedar policy generator for MCP servers"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
